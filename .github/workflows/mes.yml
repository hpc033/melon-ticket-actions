name: mes
on:
  schedule:
    - cron: '*/1 * * * *' # Every 5 minutes
jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Check Tickets
        uses: mooyoul/melon-ticket-actions@v1.1.0
        with:
          product-id: '211159'
          schedule-id: '100002'
          seat-id: '1_0'
          slack-incoming-webhook-url: ${{ secrets.TEST }}
          message: '有票了!!'

