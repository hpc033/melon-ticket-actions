name: getticket

on:
  workflow_dispatch:


jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check Tickets
        uses: hpc033/melon-ticket-actions@v1.1.0
        with:
          product-id: 211358
          schedule-id: 100001
          seat-id: 1_0
          slack-incoming-webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          message: '有票了~'
          
